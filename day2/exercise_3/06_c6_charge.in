&INPUTPP
  
  ! Same prefix as the SCF calculation
  prefix = 'c6'

  ! Same output directory as the SCF calculation
  outdir = './out'

  ! Arbitrary output filename
  filplot = 'C6.charge'

  ! Get the charge density
  plot_num = 0
/
